Submit answers to the current quiz and see your score.

Run `python3 ~/.bryonics/current/lib/quiz_submit.py $ARGUMENTS` and display the results.

Usage: /quiz-submit 1:A 2:B 3:C 4:D 5:B

The quiz ID is remembered from the last /quiz command. No need to specify it.
