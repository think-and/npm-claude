Sync current repository's code to think& server memory.

Run `python3 ~/.bryonics/current/lib/code_sync.py` and display the results.

First run: uploads all code files (summaries + content chunks).
Subsequent runs: only uploads changed files (incremental via file hash).
